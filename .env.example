# Environment variables required on the Raspberry Pi.
# Copy to /home/atsushi/.env and fill in real values.
# chmod 600 /home/atsushi/.env — owner read/write only.
# Never commit .env — only this template is versioned.

ALTCHA_HMAC_SECRET=
OPENWEATHERMAP_API_KEY=
CF_TUNNEL_TOKEN=
GITHUB_PAT=
